[user]
    name = Marius ANDRE
    email = marius.andre@epita.fr
[color]
    ui = auto
[core]
    excludesFile = ~/.gitignore
    pager = delta
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true  # use n and N to move between diff sections
    line-numbers = true
[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default
